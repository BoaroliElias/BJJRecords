# BJJRecords — app Flutter

Pasta reservada para o projeto mobile. Para criar o app:

```bash
cd mobile
flutter create .
```

O cliente deve consumir a API em `backend/BJJRecords.Api` (por exemplo `http://localhost:5080` em desenvolvimento).
